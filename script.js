# TJ Portfolio — GitHub Pages Edition

This version requires **no npm, Node, build step, or dependencies**.

## Upload directly to GitHub

1. Create a new public GitHub repository, such as `portfolio`.
2. Click **Add file → Upload files**.
3. Upload everything inside this folder, keeping the `assets` folder intact.
4. Commit the files.
5. Open **Settings → Pages**.
6. Under **Build and deployment**, choose **Deploy from a branch**.
7. Select the `main` branch and `/ (root)`, then click **Save**.

GitHub will provide a live URL after a minute or two.

## Personalize before publishing

Search `index.html` and `script.js` for:

- `YOUR_EMAIL@example.com`
- placeholder `href="#"` links
- project text you want to revise

Replace `assets/TJ-Blechman-Resume.pdf` with your actual résumé, using the same filename.

## Local preview

Double-click `index.html`. It runs directly in a browser.
